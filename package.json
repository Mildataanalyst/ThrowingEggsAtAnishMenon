{
  "name": "throw-eggs-at-anish",
  "version": "8.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/game.js && node --check src/audio.js && node --check src/difficulty.js && node scripts/check.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "description": "V8 Premium old-feel hybrid with a ten-hit Level 3, exact forced dodges, and non-resetting story interruptions."
}
