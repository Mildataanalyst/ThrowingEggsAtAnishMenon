{
  "name": "throw-eggs-at-anish",
  "version": "8.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/game.js && node --check src/audio.js && node --check src/difficulty.js && node scripts/check.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "description": "Final three-level V8 build with a global egg counter, coherent activation story, and a concealed faster cash chase."
}
