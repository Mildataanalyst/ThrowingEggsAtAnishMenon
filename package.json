{
  "name": "throw-eggs-at-anish",
  "version": "8.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/game.js && node --check src/audio.js && node scripts/check.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "description": "A three-level, portrait-first birthday game starring Anish Menon."
}